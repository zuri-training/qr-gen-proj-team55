from django.apps import AppConfig

# to check all app created -python manage.py check


class AuthenticationConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "authentication"
